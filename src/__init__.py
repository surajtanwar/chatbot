"""Pathfinder — Career Counseling Agent (v2).

A LangGraph agent designed for effortless interaction: form-first intake,
quick-reply choices, and suggested questions on every turn. Each agent turn
returns a structured :class:`~src.state.AgentResponse` envelope so any frontend
(CLI, Streamlit, web, mobile) can render forms, buttons, and chips without
knowing anything about the graph.
"""
